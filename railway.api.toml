[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python -m convictionos.commands api --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health/live"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
